# Compose local demo only. Replace with a long random alphanumeric value.
# Avoid URL-special characters here because Compose embeds this value in DATABASE_URL.
POSTGRES_PASSWORD=replace_with_a_random_alphanumeric_password
